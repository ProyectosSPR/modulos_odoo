<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Tree View -->
    <record id="view_mercadolibre_order_sync_config_tree" model="ir.ui.view">
        <field name="name">mercadolibre.order.sync.config.tree</field>
        <field name="model">mercadolibre.order.sync.config</field>
        <field name="arch" type="xml">
            <tree string="Configuraciones de Sincronizacion">
                <field name="name"/>
                <field name="account_id"/>
                <field name="interval_number"/>
                <field name="interval_type"/>
                <field name="period"/>
                <field name="use_webhook" widget="boolean_toggle" optional="show"/>
                <field name="sync_all_logistic_types" optional="hide"/>
                <field name="logistic_type_ids" widget="many2many_tags" optional="show"/>
                <field name="last_run"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'active'"
                       decoration-warning="state == 'paused'"
                       decoration-muted="state == 'draft'"/>
                <field name="active"/>
            </tree>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_mercadolibre_order_sync_config_form" model="ir.ui.view">
        <field name="name">mercadolibre.order.sync.config.form</field>
        <field name="model">mercadolibre.order.sync.config</field>
        <field name="arch" type="xml">
            <form string="Configuracion de Sincronizacion">
                <field name="cron_id" invisible="1"/>
                <field name="company_id" invisible="1"/>
                <header>
                    <button name="action_run_now" type="object" string="Sincronizar Ahora"
                            class="btn-primary" icon="fa-refresh"/>
                    <button name="action_activate" type="object"
                            string="Activar"
                            attrs="{'invisible': [('state', '=', 'active')]}"
                            class="btn-secondary"/>
                    <button name="action_pause" type="object"
                            string="Pausar"
                            attrs="{'invisible': [('state', '!=', 'active')]}"
                            class="btn-secondary"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,active,paused"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Nombre de configuracion"/>
                        </h1>
                    </div>
                    <group>
                        <group string="Cuenta">
                            <field name="account_id"/>
                            <field name="active"/>
                            <field name="use_webhook" widget="boolean_toggle"/>
                        </group>
                        <group string="Intervalo de Sincronizacion">
                            <label for="interval_number" string="Ejecutar cada"/>
                            <div class="o_row">
                                <field name="interval_number" class="oe_inline"/>
                                <field name="interval_type" class="oe_inline"/>
                            </div>
                        </group>
                    </group>
                    <div class="alert alert-info" role="alert" attrs="{'invisible': [('use_webhook', '=', False)]}">
                        <strong><i class="fa fa-plug"/> Webhook Activo:</strong>
                        Cuando lleguen notificaciones de ordenes via webhook, esta configuracion sera evaluada
                        segun el tipo logistico de la orden. Si coincide, se usara para procesar la orden.
                    </div>
                    <group>
                        <group string="Filtros">
                            <field name="period"/>
                            <field name="status_filter"/>
                            <field name="limit"/>
                            <field name="create_sale_orders"/>
                        </group>
                        <group string="Estado">
                            <field name="last_run"/>
                            <field name="last_sync_count"/>
                            <field name="next_run"/>
                        </group>
                    </group>

                    <group string="Filtro por Tipo Logistico" attrs="{'invisible': [('create_sale_orders', '=', False)]}">
                        <group>
                            <field name="sync_all_logistic_types"/>
                        </group>
                        <group attrs="{'invisible': [('sync_all_logistic_types', '=', True)]}">
                            <field name="logistic_type_ids" widget="many2many_tags"
                                   options="{'color_field': 'active'}"
                                   attrs="{'required': [('sync_all_logistic_types', '=', False)]}"/>
                        </group>
                        <div class="alert alert-info col-12" role="alert"
                             attrs="{'invisible': [('sync_all_logistic_types', '=', True)]}">
                            <strong>Nota:</strong> Solo se crearan ordenes de venta para los tipos logisticos seleccionados.
                            Las ordenes de otros tipos se sincronizaran pero no se convertiran automaticamente a ordenes Odoo.
                            La configuracion de cada tipo logistico (almacen, equipo, tarifa, etc.) tendra prioridad sobre los valores por defecto de esta configuracion.
                        </div>
                    </group>
                    <group string="Configuracion de Ordenes Odoo" attrs="{'invisible': [('create_sale_orders', '=', False)]}">
                        <group string="Automatizacion">
                            <field name="auto_confirm_order"/>
                            <field name="auto_confirm_picking"
                                   attrs="{'invisible': [('auto_confirm_order', '=', False)]}"/>
                            <field name="set_done_from_reserved"
                                   attrs="{'invisible': [('auto_confirm_picking', '=', False)]}"/>
                        </group>
                        <group string="Agrupacion y Descuentos">
                            <field name="group_by_pack"/>
                            <field name="sync_discounts"/>
                        </group>
                    </group>
                    <group string="Valores por Defecto" attrs="{'invisible': [('create_sale_orders', '=', False)]}">
                        <group>
                            <field name="default_warehouse_id"/>
                            <field name="default_pricelist_id"/>
                            <field name="default_team_id"/>
                        </group>
                    </group>
                    <group string="Productos por Defecto" attrs="{'invisible': [('create_sale_orders', '=', False)]}">
                        <field name="default_customer_id"/>
                        <field name="default_product_id"/>
                        <field name="discount_product_id"/>
                    </group>
                    <notebook attrs="{'invisible': [('create_sale_orders', '=', False)]}">
                        <page string="Manejo Aporte ML (Co-fondeo)" name="meli_cofunding">
                            <group>
                                <group string="Configuracion">
                                    <field name="meli_discount_handling"/>
                                </group>
                                <group string="Productos y Clientes"
                                       attrs="{'invisible': [('meli_discount_handling', '=', 'ignore')]}">
                                    <field name="meli_discount_product_id"
                                           attrs="{'required': [('meli_discount_handling', '!=', 'ignore')]}"/>
                                    <field name="meli_discount_partner_id"
                                           attrs="{'invisible': [('meli_discount_handling', '!=', 'separate_order')],
                                                   'required': [('meli_discount_handling', '=', 'separate_order')]}"/>
                                </group>
                            </group>
                            <div class="alert alert-info" role="alert">
                                <strong>Informacion sobre Co-fondeo:</strong>
                                <ul>
                                    <li><b>No registrar:</b> El aporte de MercadoLibre no se registra en Odoo</li>
                                    <li><b>Linea en la misma orden:</b> Se agrega una linea positiva adicional en la orden del cliente</li>
                                    <li><b>Orden de venta separada:</b> Se crea una orden de venta independiente con el cliente especificado (ej: "MercadoLibre")</li>
                                </ul>
                                <p class="mb-0">El aporte de ML es la parte del descuento que MercadoLibre subsidia. Registrarlo permite ver el ingreso real de la venta.</p>
                            </div>
                        </page>
                    </notebook>
                    <group string="Ultima Sincronizacion">
                        <field name="last_sync_log" readonly="1" nolabel="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_mercadolibre_order_sync_config" model="ir.actions.act_window">
        <field name="name">Configuracion Sync</field>
        <field name="res_model">mercadolibre.order.sync.config</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Configure la sincronizacion automatica de ordenes
            </p>
            <p>
                Cree configuraciones de sincronizacion para cada cuenta de MercadoLibre.
                Puede configurar el intervalo de sincronizacion por minutos, horas o dias.
            </p>
        </field>
    </record>
</odoo>
