<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="mail_template_mercadolibre_invitation" model="mail.template">
        <field name="name">MercadoLibre: Invitation</field>
        <field name="model_id" ref="model_mercadolibre_invitation"/>
        <field name="subject">Invitación para conectar tu cuenta de MercadoLibre</field>
        <field name="email_from">${(object.company_id.email or user.email_formatted) | safe}</field>
        <field name="email_to">${object.email | safe}</field>
        <field name="body_html" type="html">
            <div style="margin: 0px; padding: 0px; font-family: Arial, Helvetica, sans-serif; font-size: 14px;">
                <p>Hola,</p>
                <p>Has sido invitado a conectar tu cuenta de MercadoLibre con <strong>${object.company_id.name}</strong>.</p>
                <p>Para autorizar tu cuenta, haz clic en el siguiente botón:</p>
                <p style="text-align: center; margin: 20px 0;">
                    <a href="${object.authorization_url}" style="padding: 10px 20px; background-color: #FFE600; color: #000000; text-decoration: none; border-radius: 4px; font-weight: bold;">
                        Conectar con MercadoLibre
                    </a>
                </p>
                <p>O copia y pega el siguiente enlace en tu navegador:</p>
                <p><a href="${object.authorization_url}">${object.authorization_url}</a></p>
                <p><strong>Nota:</strong> Esta invitación expirará en 7 días.</p>
                <p>Saludos,<br/>${object.company_id.name}</p>
            </div>
        </field>
        <field name="auto_delete" eval="False"/>
    </record>
</odoo>
