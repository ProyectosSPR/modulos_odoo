<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!--
        NOTA: Los crons de sincronización se crean dinámicamente por cada
        configuración cuando se activa. Ver action_activate() en
        mercadolibre_billing_sync_config.py

        Cada configuración tiene su propio cron individual con el intervalo
        configurado por el usuario.
        -->
    </data>
</odoo>
